# Dev-mode container (Vite dev server) — sufficient for a POC demo.
# A production image would build static assets and serve them via nginx.
FROM node:20-slim

WORKDIR /app

COPY frontend/package.json frontend/package-lock.json* ./
RUN npm install

COPY frontend .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
