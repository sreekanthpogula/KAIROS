# Core runtime dependencies. Everything here is required for KCIP to run
# end-to-end in DEMO_MODE with zero external services (SQLite + local
# deterministic embeddings) AND against a real Postgres deployment (Neon,
# Supabase, Vercel, docker-compose). See requirements-optional.txt for
# extras (pgvector's native ANN path, sentence-transformers, OCR) that are
# architecturally supported but not required to run the POC.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
python-multipart==0.0.20
PyYAML==6.0.2
psycopg[binary]==3.2.3

# Document extraction
PyMuPDF==1.25.1
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2
beautifulsoup4==4.12.3
lxml==5.3.0

# Retrieval / scoring
numpy==2.2.1
rank-bm25==0.2.2

# HTTP client for LLM_MODE (OpenAI-compatible providers) and the HTTP connector
httpx==0.28.1

# Synthetic sample-corpus generation (scripts/seed_demo_data.py)
reportlab==4.2.5

# Connector catalog (app/connectors/) — S3-compatible object storage, and
# Google-specific connectors modeled after Vertex AI Search's "Data Store +
# connector" concept (Cloud Storage buckets, Drive files/folders)
boto3==1.35.99
google-cloud-storage==2.19.0
google-api-python-client==2.159.0
google-auth==2.37.0

# Testing
pytest==8.3.4
pytest-cov==6.0.0
moto[s3]==5.0.28
